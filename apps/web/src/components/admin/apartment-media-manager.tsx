"use client";import{useCallback,useEffect,useState}from"react";import{adminFetch}from"@/lib/admin-api";import type{AdminUserDto,MediaAssetDto}from"@/lib/types";import{ApartmentImageUpload}from"./apartment-image-upload";import{ApartmentMediaCard}from"./apartment-media-card";
export function ApartmentMediaManager({slug,code}:{slug:string;code:string}){const[media,setMedia]=useState<MediaAssetDto[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[role,setRole]=useState<AdminUserDto["role"]>("SALES");const load=useCallback(()=>{setLoading(true);setError("");return Promise.all([adminFetch<{media:MediaAssetDto[]}>(`/apartments/${slug}/media`),adminFetch<{admin:AdminUserDto}>("/auth/me")]).then(([data,me])=>{setMedia(data.media);setRole(me.admin.role)}).catch(e=>setError(e instanceof Error?e.message:"Slike trenutno nije moguće učitati. Pokušajte ponovo.")).finally(()=>setLoading(false))},[slug]);useEffect(()=>{void Promise.resolve().then(load)},[load]);const readOnly=role==="SALES";async function move(index:number,direction:-1|1){const target=index+direction;if(target<0||target>=media.length)return;const reordered=[...media];[reordered[index],reordered[target]]=[reordered[target],reordered[index]];setMedia(reordered);try{await adminFetch(`/apartments/${slug}/media/reorder`,{method:"POST",body:JSON.stringify({items:reordered.map((x,i)=>({id:x.id,sortOrder:i}))})});await load()}catch(e){setError(e instanceof Error?e.message:"Redosled nije sačuvan.");await load()}}return <section className="mt-6 rounded-2xl bg-white p-6 shadow-sm"><h2 className="text-2xl font-semibold">Slike apartmana {code}</h2><div className="mt-5"><ApartmentImageUpload slug={slug} code={code} hasCover={media.some(x=>x.isCover)} onUploaded={load} readOnly={readOnly}/></div>{loading?<div className="mt-6 h-48 animate-pulse rounded-2xl bg-stone-100"/>:error?<div className="mt-6 rounded-xl bg-red-50 p-4"><p>Slike trenutno nije moguće učitati. Pokušajte ponovo.</p><button onClick={load}>Pokušajte ponovo</button></div>:media.length===0?<p className="mt-6">Apartman trenutno nema uploadovane slike.</p>:<div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{media.map((item,i)=><ApartmentMediaCard key={item.id} item={item} onChanged={load} onMove={d=>void move(i,d)} readOnly={readOnly}/>)}</div>}</section>}
