"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { adminFetch } from "@/lib/admin-api";
export default function AdminLogin(){const r=useRouter();const[e,setE]=useState('');const[p,setP]=useState('');const[err,setErr]=useState('');return <main className="flex min-h-screen items-center justify-center bg-forest-950 p-6"><form className="w-full max-w-md rounded-3xl bg-white p-8 shadow" onSubmit={async x=>{x.preventDefault();setErr('');try{await adminFetch('/auth/login',{method:'POST',body:JSON.stringify({email:e,password:p})});r.push('/admin')}catch(a){setErr(a instanceof Error?a.message:'Prijava nije uspela.')}}}><p className="text-sm uppercase tracking-[0.3em] text-forest-700">Forest Glade</p><h1 className="mt-2 text-3xl font-semibold">Admin prijava</h1><input className="mt-8 w-full rounded-xl border p-3" placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input className="mt-3 w-full rounded-xl border p-3" placeholder="Lozinka" type="password" value={p} onChange={x=>setP(x.target.value)}/>{err&&<p className="mt-3 text-sm text-red-700">{err}</p>}<button className="mt-6 w-full rounded-xl bg-forest-900 p-3 font-semibold text-white">Prijavi se</button></form></main>}
