"use client";
import{useEffect,useState}from"react";import{AdminShell}from"@/components/admin/admin-shell";import{adminFetch,statusLabels}from"@/lib/admin-api";
export default function Page(){const[l,setL]=useState<any[]>([]);const[q,setQ]=useState('');const[st,setSt]=useState('');const load=()=>adminFetch<any>(`/leads?q=${q}&status=${st}`).then(d=>setL(d.leads));useEffect(()=>{void load()},[q,st]);async function upd(id:string,status:string,note?:string){await adminFetch(`/leads/${id}`,{method:'PATCH',body:JSON.stringify({status,note})});load()}return <AdminShell><h1 className="text-3xl font-semibold">Leadovi</h1><div className="my-6 flex gap-3"><input className="rounded-xl border p-3" placeholder="Pretraga" onChange={e=>setQ(e.target.value)}/><select className="rounded-xl border p-3" onChange={e=>setSt(e.target.value)}><option value="">Svi</option>{Object.keys(statusLabels).slice(4).map(s=><option key={s}>{s}</option>)}</select></div><div className="grid gap-4">{l.map(x=><div className="rounded-2xl bg-white p-5" key={x.id}><b>{x.name}</b> {x.phone} {x.email}<p>{x.message}</p><small>{new Date(x.createdAt).toLocaleString('sr-RS')}</small><div className="mt-3 flex gap-2"><select defaultValue={x.status} onChange={e=>upd(x.id,e.target.value,x.note)}>{['NEW','CONTACTED','QUALIFIED','NOT_INTERESTED','CLOSED','ARCHIVED'].map(s=><option key={s}>{s}</option>)}</select><input placeholder="Interna beleška" defaultValue={x.note??''} onBlur={e=>upd(x.id,x.status,e.target.value)}/></div></div>)}</div></AdminShell>}
