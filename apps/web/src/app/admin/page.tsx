"use client";
import { useEffect,useState } from "react"; import { AdminShell } from "@/components/admin/admin-shell"; import { adminFetch } from "@/lib/admin-api";
export default function AdminHome(){const[d,setD]=useState<any>();useEffect(()=>{adminFetch('/dashboard').then(setD)},[]); const cards=[['Novi leadovi',d?.newLeads],['Ukupno leadova',d?.totalLeads],['Upload media',d?.totalMedia],['Objavljeno media',d?.publishedMedia],['Slobodni apartmani',d?.apartmentsByStatus?.find((x:any)=>x.status==='AVAILABLE')?._count],['Prodati',d?.apartmentsByStatus?.find((x:any)=>x.status==='SOLD')?._count]];return <AdminShell><h1 className="text-3xl font-semibold">Dashboard</h1><div className="mt-8 grid gap-4 md:grid-cols-3">{cards.map(([k,v])=><div className="rounded-2xl bg-white p-6" key={k as string}><p className="text-sm text-stone-500">{k}</p><p className="mt-3 text-3xl font-bold">{v??'—'}</p></div>)}</div><h2 className="mt-10 text-xl font-semibold">Poslednje aktivnosti</h2></AdminShell>}
