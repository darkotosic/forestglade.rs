import type { MediaPlacement } from "./types";
export type CatalogFilenameResult={slug:string|null;placement:Extract<MediaPlacement,"APARTMENT_CATALOG"|"APARTMENT_GALLERY"|"INTERIOR">;isCover:boolean;confident:boolean};
export function parseCatalogFilename(filename:string):CatalogFilenameResult{const base=filename.toLowerCase().replace(/\.[^.]+$/,'');let number:number|null=null;const page=base.match(/(?:catalog-page|katalog-strana|(?:^|[-_ ])page)[-_ ]*(\d{1,2})(?:$|[-_ ])/);if(page)number=Number(page[1])-9;else{const apartment=base.match(/^(?:stan|apartman|a)[-_ ]*0*(\d{1,2})(?:$|[-_ ])/);if(apartment)number=Number(apartment[1]);}const valid=number!==null&&number>=1&&number<=31;const extra=/(?:enterijer|interior)/.test(base);const additional=/^a0*\d{1,2}[-_ ](?:\d+|enterijer|interior)/.test(base);return{slug:valid?`a${number}`:null,placement:extra?"INTERIOR":additional?"APARTMENT_GALLERY":"APARTMENT_CATALOG",isCover:valid&&!additional,confident:valid};}
