import { apiPath } from "./api";
export async function adminFetch<T>(path: string, init: RequestInit = {}): Promise<T> { const r=await fetch(apiPath(`/admin${path}`),{...init,credentials:"include",headers:{...(init.body instanceof FormData?{}:{"Content-Type":"application/json"}),...(init.headers??{})}}); const d=await r.json().catch(()=>null); if(!r.ok||!d?.ok) throw new Error(d?.message??"Admin zahtev nije uspeo."); return d as T; }
export const statusLabels: Record<string,string>={AVAILABLE:"Slobodan",RESERVED:"Rezervisan",SOLD:"Prodat",HIDDEN:"Sakriven",NEW:"Nov",CONTACTED:"Kontaktiran",QUALIFIED:"Kvalifikovan",NOT_INTERESTED:"Nije zainteresovan",CLOSED:"Zatvoren",ARCHIVED:"Arhiviran"};
